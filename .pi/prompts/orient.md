---
description: Orient in the vault — structure, agents, recent activity
---

Orient yourself in this Obsidian vault, then report back briefly:

1. Read the vault-level `AGENTS.md` context you were given (structure, write boundary, Obsidian CLI).
2. List the top-level folders and one line on what each holds.
3. List the specialized agents under `AGENTS/12 PI/agents/` and what each is for.
4. Run `git log --oneline -5` and summarize recent vault activity.

Keep the whole report under 30 lines. Do not write any files.
